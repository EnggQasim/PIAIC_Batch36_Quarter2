# Installation pycaret
* `python 3.6 - 3.8'
    * now we create conda virtual env with **python 3.6**
    * ``` conda create -n test_env python=3.6.3 anaconda ```
        * ```conda activate test_env```